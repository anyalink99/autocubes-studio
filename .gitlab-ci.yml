stages:
  - verify
  - build

typecheck:
  stage: verify
  tags: [vps]
  script:
    - docker build --target verify -t "autocubes-studio-verify:$CI_COMMIT_SHA" .

build:
  stage: build
  tags: [vps]
  script:
    - docker build --target runtime -t "autocubes-studio:$CI_COMMIT_SHA" .
